<!-- This needs to change, currently passing 4mb worth of data through a tag -->
<canvas id="grid" height="<%= @grid.height %>", width="<%= @grid.width %>" data-pixel-array="<%= @grid.pixel_array.to_json %>"></canvas>
